# Local development only. Do not place credentials here.
# Prefer Airflow Connections / a Secrets Backend for credentials and endpoints.
AIRFLOW_HOME=./.airflow
PYTHONPATH=./src

# Optional local-only tuning, non-secret values only:
# AIRFLOW__CORE__LOAD_EXAMPLES=False
